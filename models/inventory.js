class Assembly {
    constructor(worker, task, schedule, pay ) {
        this.worker = worker;
        this.task = task;
        this.schedule = schedule;
        this.pay = pay;
    }
}

module.exports = Assembly;